# include <iostream>
# include <math.h>
using namespace std;

double derivada (int a, int b , int N ){    
}

int main (){
    
    double a = 0; // inicio del intervalo
    double b =1; // final del intervalo
    int N; // numero de puntos a dividir
    cout <<"ingrese el inicio del intervalo";
    cin>> a;
    cout << "final del intervalo ";
    cin >> b ;
    cout << "ingrese el numero de puntos";
    cin>> N;
    int deri[N]; // arreglo con longitud puntos generados 
    int x [N]
    double coseno[N];
    int *point;
    
    // evaluar en coseno 
       for (int i = a, i <b; i++){
           double c[]; 
           double cos; 
           double x[i]= c; // recorra de a hasta b y guarde en c 
           coseno[c] = cos(c); // guarde en cos[c], los valores evaluados 
           c+=1;
           return c;
       }
    derivada (a,b,N);    
}
double derivada (int a, int b , int N ){
    for (i=a; i<b,i++){
        int deri[i]=(b-a)/N;
        cout<< deri[i]<< endl // retorna el arreglo de derivada
    }    
}
